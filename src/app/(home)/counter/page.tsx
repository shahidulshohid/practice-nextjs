import Counter from "./counter";

export const metadata = {
    title: 'Counter'
}
const CounterPage = () => {
    return (
        <div>
            <Counter/>
        </div>
    );
};

export default CounterPage;